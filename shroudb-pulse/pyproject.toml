[project]
name = "shroudb-pulse"
version = "0.1.0"
description = "Python client for the ShroudbPulse Observability plane for unified audit event streaming"
requires-python = ">=3.10"
license = "MIT"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
