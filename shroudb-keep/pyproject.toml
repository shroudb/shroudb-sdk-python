[project]
name = "shroudb-keep"
version = "0.1.0"
description = "Python client for the ShroudbKeep Secrets manager"
requires-python = ">=3.10"
license = "MIT"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
