[project]
name = "shroudb-auth"
version = "0.1.0"
description = "Python client for the ShroudbAuth Authentication service"
requires-python = ">=3.10"
license = "MIT"
dependencies = [
    "httpx>=0.25",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
